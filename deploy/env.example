# Copiar como ".env" en ~/vantio-monitoreo (chmod 600). NO va a git.
# Lo consume docker-compose.yml para crear el contenedor de PostgreSQL.
POSTGRES_DB=lexmark_monitor
POSTGRES_USER=lexmark_user
POSTGRES_PASSWORD=REEMPLAZAR
